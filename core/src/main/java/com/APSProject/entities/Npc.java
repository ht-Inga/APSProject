package com.APSProject.entities;

import com.APSProject.components.*;

public class Npc extends Entity{
    public Npc(
        long id,
        Position position,
        Sprite path
    ) {
        super(
            id,
            position,
            path
        );
    }
}
